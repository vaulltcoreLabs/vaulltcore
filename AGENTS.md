# Vaulltcore repo memory

- Monorepo: npm workspaces; packages export `src/index.ts` directly (no build step; vitest
  resolves TS). `npm test` = vitest, `npm run typecheck` = `tsc --build packages/*`.
- Phase 1A delivered a durable job runner; full report in `docs/phase1a.md`.
- Hard seam: `packages/vaulltcore-runner` (neutral, zero runtime deps) must never import
  from `packages/vaulltcore-runner-opencode` (adapter). Engine-specific types must not
  leak into the neutral contracts.
- Durability rules that must not regress: tool calls recorded before execution; commit
  boundary = `checkpoint` event + checksummed checkpoint file; resume validates checksum,
  identity, policy/engine versions, watermark before continuing; recorded-but-unsettled
  tool calls are never blindly re-executed.
- Deterministic engines (`ScriptEngine`, `ScriptModelProvider`) derive their step index
  from committed history (assistant message count), not instance state — fresh instances
  must resume correctly.
- Extracted OpenCode code is MIT; keep attribution headers and `NOTICE.md` current.

Phase 1C COMPLETE: 76/76 tests, report docs/phase1c.md.
- New packages: `packages/vaulltcore-store-sql` (transactional DurableJobStore over node:sqlite via SqlDatabase/SqlDialect seam -- Postgres-ready), `packages/vaulltcore-environment-cloud` (vendor-neutral CloudExecutionProvider + CloudExecutionEnvironment + FakeCloudProvider), `packages/vaulltcore-control` (thin HTTP facade over AgentRunner: POST/GET /jobs, SSE events, cancel, input, usage; replaceable auth boundary; Idempotency-Key registry).
- Runner contract additions (backward compatible): `getJob`, `listEvents`; `JobView` read model; `snapshot-policy.ts` (SnapshotPolicy/ThresholdSnapshotPolicy; advisory only, emits sanitized "snapshot_decision" warning events).
- Invariants that must not regress: SQL store keeps fenced CAS on every state-changing write; (job_id, seq) PRIMARY KEY rejects duplicate delivery; orphan events beyond checkpoint watermark never replay as committed history; cloud snapshot is optimization only -- checkpoint + event log stay authoritative; capability honesty: unsupported native ops throw CapabilityUnsupportedError, never pretend; control plane derives tenant from the authenticated principal, never from request body; snapshot policy can never suppress checkpoint durability.
- node:sqlite is experimental in Node 22: it is loaded via createRequire in store-sql/src/driver.ts so bundlers (vite/vitest) do not try to resolve the stripped "sqlite" id. Do not replace with a static import.
- Test loop gotcha: the runner loop only continues past a turn when the turn emitted tool calls; a text-only turn is terminal. Tests that need multi-turn jobs must give turn 0 a tool call.

Phase 1B COMPLETE (AX extraction): contracts extended in packages/vaulltcore-runner/src/contracts.ts (SuspensionReason, WorkspaceState, ExecutionSnapshot, ExecutionEnvironment, OwnershipGrant, ActorHandle, RecoveryContext, ExecutionActorController, JobRecord.latestSnapshot). environment.ts (LocalExecutionEnvironment: sha256(jobId) binding, manifest+per-file hash snapshots, same-host reattach). actor.ts (ExecutionActorControllerImpl: acquire/start/suspend/recover/resume/snapshot/release/destroy; recovery algorithm: validate→fence→checkpoint→events→workspace→snapshot→proceed; invalid continuation parks suspended). Store fenced appends (expectedAttempt) + releaseLease. Runner routes through controller. Tests 34/34; report docs/phase1b-actors.md.

Phase 1C COMPLETE: 76/76 tests, report docs/phase1c.md.
- New packages: `packages/vaulltcore-store-sql` (transactional DurableJobStore over node:sqlite via SqlDatabase/SqlDialect seam — Postgres-ready), `packages/vaulltcore-environment-cloud` (vendor-neutral CloudExecutionProvider + CloudExecutionEnvironment + FakeCloudProvider), `packages/vaulltcore-control` (thin HTTP facade over AgentRunner: POST/GET /jobs, SSE events, cancel, input, usage; replaceable auth boundary; Idempotency-Key registry).
- Runner contract additions (backward compatible): `getJob`, `listEvents`; `JobView` read model; `snapshot-policy.ts` (SnapshotPolicy/ThresholdSnapshotPolicy; advisory only, emits sanitized "snapshot_decision" warning events).
- Invariants that must not regress: SQL store keeps fenced CAS on every state-changing write; (job_id, seq) PRIMARY KEY rejects duplicate delivery; orphan events beyond checkpoint watermark never replay as committed history; cloud snapshot is optimization only — checkpoint + event log stay authoritative; capability honesty: unsupported native ops throw CapabilityUnsupportedError, never pretend; control plane derives tenant from the authenticated principal, never from request body; snapshot policy can never suppress checkpoint durability.
- node:sqlite is experimental in Node 22: it is loaded via createRequire in store-sql/src/driver.ts so bundlers (vite/vitest) do not try to resolve the stripped "sqlite" id. Do not replace with a static import.
- Test loop gotcha: the runner loop only continues past a turn when the turn emitted tool calls; a text-only turn is terminal. Tests that need multi-turn jobs must give turn 0 a tool call.

Phase 1D COMPLETE: 116/116 tests, report docs/phase1d.md.
- New packages: `packages/vaulltcore-worker` (WorkerHost, WorkerIdentity, fenced WorkerLease + heartbeat loop, JobReconciler), `packages/vaulltcore-environment-docker` (real DockerCloudProvider over the Docker Engine).
- New runner contracts (backward compatible): `packages/vaulltcore-runner/src/dispatcher.ts` (neutral JobDispatcher: enqueue/claim/acknowledge/heartbeat/release — the dispatcher decides where work goes, the runner decides how it executes), `packages/vaulltcore-runner/src/distributed.ts` (recovery eligibility; `RecoveryEligibilityReason` gains "orphaned").
- New store-sql: `SqlIdempotencyRegistry` + `SqlSnapshotRegistry` (`registries.ts`), `SqlDispatcher` + `PostgresDispatcher` (`dispatcher.ts`/`pg-dispatcher.ts`), `DistributedSqlStore` (`distributed-store.ts`), `PostgresJobStore` + `pg-migrations.ts` over the `pg` driver with SERIALIZABLE + FOR UPDATE fencing and int8-as-number parsing.
- Phase 1D rule proven: the HTTP process is never the long-running job owner; a request transactionally persists job + idempotency record then dispatches. Control plane and workers restart independently without losing job correctness.
- Invariants that must not regress: lease renewal is fenced (generation N-1 can never write once generation N is committed, across separate connections / a partition); recovery never blindly reruns uncertain non-idempotent tool calls; a superseded snapshot is collected ONLY when its replacement is `active` (not merely `created`) — never delete the last valid recovery artifact before its replacement is durably committed; DockerCloudProvider reports `durableWorkspace=false` honestly and throws CapabilityUnsupportedError for a missing image (never emulates); the worker requires no live HTTP server.
- Gated tests: postgres-conformance.test.ts and docker-provider.test.ts SKIP (not fail) when PostgreSQL/Docker is unavailable, so CI without those services stays green. Defaults: PG on unix socket /tmp/pgsock:5434 db vaulltcore_test; Docker via `DOCKER_CMD` (e.g. `sudo -n docker`). The root typecheck now builds `packages/*` (all packages, not just runner+opencode).

Phase 1E COMPLETE: 98/98 tests, report docs/phase1e.md.
- New packages: vaulltcore-identity (Tenant/Org/Member/Project/ProjectGrant/APIKey; API-key verifier hash + `.` separator, never plaintext; org/project-scoped authorize), vaulltcore-policy (versioned AdmissionPolicy -> immutable AdmissionDecision -> runner ExecutionPolicy; partial UNIQUE active index), vaulltcore-quota (race-free reservation state machine active/settled/released/expired/rejected; conditional in_use increment + UNIQUE request_key; fenced version on every transition), vaulltcore-metering (append-only idempotent UsageEvent; UNIQUE (tenant,job,kind,dedup_key); eventsToUsage adapter derives dedup keys from committed event seq), vaulltcore-billing (versioned PricingVersion; immutable LedgerEntry; UNIQUE (tenant,org,project,idempotency_key); no external payment processor), vaulltcore-audit (append-only; sanitizeMetadata redacts secret keys + opaque secret values; no update/delete path).
- Control-plane admission pipeline in vaulltcore-control/src/admission.ts (AdmissionPipeline): authenticate -> authorize -> idempotency -> policy.evaluate -> quota.reserve -> runner.createJob with compensation (release on creation failure). Extends, does not replace, existing facade.
- Invariants that must not regress: business layer never embedded in runner loop; tenant derived from authenticated principal never request body; least-privilege projectScope -- a principal with NO project grants has access to NO projects (the wildcard is never synthesized from absence); policyVersion pinned into JobRecord/JobCheckpoint and immutable after creation; exactly-once at the durable event/ledger identity boundary via UNIQUE + idempotency (execution stays at-least-once); reservation settlement/release fenced by version and idempotent; failed admission releases reservation (no leak); no plaintext secrets in audit/metering/billing/events.
- Reuses vaulltcore-store-sql SqlStoreBase + SqlDialect seam; all business stores apply their own migrations in-constructors. PostgreSQL concurrency tests are environment-gated SKIPs (PG not available in sandbox), not false passes.

Phase 1F COMPLETE: 175 passed / 0 skipped / 0 errors / 0 warnings when PG + Docker are provisioned (verified green); otherwise 17 environment-gated tests skip honestly. report docs/phase1f.md.
- Green-run provisioning: PostgreSQL on unix socket /tmp/pgsock:5434 (user postgres, db vaulltcore_test, trust auth) via `initdb -D /tmp/pgdata -U postgres --auth=trust` then `postgres -c unix_socket_directories=/tmp/pgsock -c port=5434 -c listen_addresses=''` then `createdb -h /tmp/pgsock -p 5434 -U postgres vaulltcore_test`; Docker via `dockerd` with the runner able to reach the socket (group `docker`, or `DOCKER_CMD='sudo -n docker'`). These match the test defaults; no env vars needed once provisioned.
- The Node `node:sqlite` `ExperimentalWarning` is suppressed in the `test` npm script via `NODE_OPTIONS=--disable-warning=ExperimentalWarning` (targeted: only ExperimentalWarning, never all warnings).
- New package: `packages/vaulltcore-reconcile` (ReconciliationService + SqlReconciliationStore; durable watermark as SOLE progress source; detects gaps A-H; repairs only safe missing downstream projections — rebuilds UsageEvents from committed JobEvents, retries pricing/ledger via durable idempotency, releases orphaned/terminal-unsettled reservations; NEVER re-executes agent steps or tool calls).
- New migrations: store-sql v8 `admission_idempotency` (durable distributed idempotency registry), v9 `reconciliation` (runs + gaps), v10 `snapshot_gc` (gc_attempts); billing v11 `usage_settlement` (settlement tracking); identity v12 `api_key_lifecycle` (expiry/rotation/last-used).
- Durable distributed idempotency: `SqlAdmissionIdempotencyRegistry` replaces in-memory registry; state machine pending→{completed|failed_retriable|failed_terminal}; claim() is the linearization point (UNIQUE tenant+key); stores only SHA-256 fingerprint (never secret material); same-key+different-fingerprint = explicit 409 conflict (never silent replay); replay returns original result without repeating reservation/job creation; compensation releases reservation on job-creation failure.
- Quota reservation expiry/reaper: active reservations have durable expires_at; renewReservation fenced by version (stale writer cannot extend); reapExpired idempotent + fenced (run twice = no double-release; capacity released exactly once); expired orphan after reservation-before-job-creation is recovered. Distinction documented: admission reservation (economic capacity) vs execution ownership lease (write authority) vs worker heartbeat (liveness) — NOT interchangeable.
- Durable billing settlement: UsageEvent→classify→resolve immutable PricingVersion→exactly-one LedgerEntry; states pending→priced→settled / non_billable / unresolved; unresolved is durable+retryable (never silently dropped); pricing changes cannot rewrite historical charges; ledger history immutable (corrections = new entries).
- Runtime economic enforcement: ExecutionPolicy gains optional soft maxTokens/maxDurationMs; checked at step boundaries (bounded overshoot of one turn); budget exhaustion emits budget_exhausted event + cancels through a durable state preserving the checkpoint boundary — checkpoint correctness is NEVER sacrificed for billing/quota.
- API-key lifecycle: expiry/rotation (rotated_from + overlap_expires_at)/revocation/last-used (async best-effort, never an auth bottleneck); authorization uses authoritative key state not cached last-used; never expose stored secret.
- Snapshot GC: lifecycle active→eligible_for_gc→deleting→deleted; conservative (never deletes active recovery path); retry-safe (failed deletion stays deleting); deleted only on provider confirmation; tenant-isolated.
- Operational health: tenant-scoped GET /reconcile/health + GET /operations/health (unresolved usage/pricing, orphaned reservations, settlement/snapshot-GC backlog, last watermark); ordinary tenant credential can never read another tenant's operational data.
- Migration ledger fix (cross-cutting): schema_migrations now deduplicates by migration NAME (globally unique), not version number — different packages intentionally use overlapping version numbers (identity v2 vs store-sql v2), so version-only dedup silently skipped migrations and left tables uncreated. PK changed version→name. This is what lets all stores share one database (required for single-DB reconciliation + PGlite conformance).
- PG validation: Tier A PGlite (real PostgreSQL engine, ALWAYS runs) proves SQL-level invariants — 9 passed; Tier B multi-connection server gated on PG_TEST_* (2 passed when a server is provisioned at /tmp/pgsock:5434; skipped honestly otherwise). PG skips are reported as skips, never passes.
- Invariants that must not regress: reconciliation never invokes AgentRunner execution (reads listEvents only, projects forward); no stale writer mutates ownership/reservation state (fenced CAS everywhere); no cross-tenant economic/audit read; no plaintext API keys/secrets in SQL/logs/events/audit/errors; pricing+ledger history immutable; policy/job identity immutable after admission; exactly-once at durable usage/ledger identity boundaries (execution stays at-least-once); reaper idempotent + fenced; snapshot GC never deletes active recovery artifact; failed provider deletion stays retryable (never falsely deleted).

Phase 2A COMPLETE: 208 passed / 17 environment-gated skips (10 PG + 7 Docker) / 0 failures. report docs/phase2a.md.
- New package: `packages/vaulltcore-automation` (AutomationTemplate → AutomationVersion → AutomationRun; immutable versioned definitions with checksum; typed input contracts + durable InputRevision; job orchestration via narrow `AutomationJobDispatcher` seam; vendor-neutral ArtifactStore + DeliveryProvider; fenced approval gates; at-least-once delivery with idempotent settlement; stable product events projected from runner events; reconcileRun re-projects + idempotently re-drives stuck runs).
- Dependency direction (enforced, no cycles): automation → {runner (types), store-sql (SqlStoreBase), identity (ResolvedPrincipal/Role), audit}; control → automation. The runner is NOT modified and never imports automation. The hard seam holds.
- Product vs execution aggregates: `AutomationRun` (product-level: validating→running→collecting→awaiting_approval→delivering→completed/failed/cancelled/rejected/suspended) is distinct from Phase 1 `Job` (execution-level). They never merge. `JobMapping` (runId, stepId)→jobId is the durable bridge; UNIQUE (run_id, step_id) prevents duplicate execution work.
- Invariants that must not regress: published versions are immutable (any change = new version; checksum verified on every load, corruption detected); invalid input creates no run and no job; exact historical input frozen as InputRevision (checksum, never silently replaced); every job traceable to (version, inputRevision); crash after job creation before projection recovers safely (reconcile re-projects from committed events + idempotently re-drives, never duplicates work); approval decisions fenced by approvalVersion CAS (concurrent approvers → exactly one terminal, no contradiction); awaiting_approval blocks delivery until approved; delivery is at-least-once with idempotent settlement (UNIQUE run_id+idempotency_key; in_progress before provider call; crash never falsely marks delivered); cross-tenant reads return null (no existence leak); tenant derived from authenticated principal never request body; no second authorization model (reuses Phase 1E role-rank); reconciliation never invokes agent execution (reads listJobEvents, projects forward).
- Execution graph deliberately constrained: no loops/parallel/recursion; steps in strict topological order (validateStepGraph rejects cycles, duplicate stepIds, missing/out-of-scope deps). Not a general workflow engine.
- Idempotency boundaries: run creation (tenant+idempotency_key), job dispatch (runId+stepId → auto:runId:stepId; dispatcher deduplicates), approval decision (approvalId+approvalVersion fenced CAS), delivery (runId+idempotency_key UNIQUE + fenced transition). Execution stays at-least-once; settlement is exactly-once at the durable identity boundary.
- Only Phase 1 file touched: `vaulltcore-audit/src/contracts.ts` — purely additive (new automation event types appended to AUDIT_EVENT_TYPES const; type persisted as TEXT, no schema change). No Phase 1 semantic/invariant weakened.
- New migration: automation v2 `automation_core` (name globally unique; 10 tables: automation_templates, automation_versions, automation_runs, automation_run_inputs, automation_run_steps, automation_job_mappings, automation_artifacts, approval_requests, delivery_attempts, automation_events). All state-changing writes fenced by version CAS.
- PG validation: Tier A PGlite (9 tests, ALWAYS runs) proves SQL-level invariants (UNIQUE constraints, fenced CAS, cross-tenant isolation) against real PostgreSQL. Tier B multi-connection server tests deferred to Phase 2B (matching Phase 1F gating policy).

Phase 2B COMPLETE: 275 passed / 18 environment-gated skips (10 PG + 7 Docker + 1 pglite-server) / 0 failures. report docs/phase2b.md.
- New packages: `vaulltcore-artifacts` (neutral `ProductionArtifactStore` seam + `LocalFilesystemArtifactStore` + `S3ArtifactStore` with vendored SigV4 signer over injectable RequestOptions/ClientRequest — AWS never a core dependency, works against any S3-compatible endpoint; content-addressed SHA-256 identity; idempotent put on content; verify detects corruption; idempotent delete; badRef rejected — no path traversal via contentRef; metadata sanitized; tenant/project ownership), `vaulltcore-delivery` (neutral `DeliveryProvider` + `WebhookDeliveryProvider` + `EmailDeliveryProvider` (SMTP seam) + `SlackDeliveryProvider`; `SsrfGuard` blocks loopback/RFC1918/link-local; `redactUrl` strips userinfo; `RetryPolicy`+`defaultClassifier` with 6 classes transient/rate_limited/auth_config/permanent_validation/provider_rejection/unknown_uncertain; bounded exponential backoff with full jitter, capped by maxMs+maxAttempts; failed_retriable not claimable until nextRetryAt), `vaulltcore-ops` (durable operational work queue `SqlOpsStore` + `OperationalWorker`; fenced claiming by generation — crashed worker's expired claim reaped + reclaimed by replacement; work kinds approval_expiry/delivery_retry/abandoned_run/expired_reservation/stale_idempotency/artifact_lifecycle; states pending→claimed→in_progress→{succeeded|failed_terminal|failed_retriable}; idempotent on (tenant,idempotencyKey)), `vaulltcore-scheduler` (durable `SqlScheduleStore` + `Scheduler`; one-time + recurring cron; tz-aware deterministic nextRun; pause/resume/cancel; versioned schedules with fenced publishVersion CAS; missed-run policy skip/catch_up bounded by maxCatchUp; idempotent firing via deterministic occurrenceId `occ:<scheduleId>:<scheduledTime>` + UNIQUE occurrence row — scheduler crash never duplicates a run; NOT a general workflow engine), `vaulltcore-recovery` (`RecoveryScanner` read-only + `buildReapers` for approval-expiry/delivery-retry/abandoned-run; operates around existing durable watermark; reaper idempotent on re-run; recovery never invokes agent execution — reconcileRun re-projects + idempotently re-drives via AutomationService.reconcileRun; tenant isolation).
- Control plane Phase 2B routes (additive `phase2b` layer, requires automation layer): POST/GET /automation/schedules, GET /automation/schedules/:id, pause/resume/cancel, occurrences; GET /automation/runs/:id/deliveries; SSE GET /automation/runs/:id/stream?after=&follow= (replay no-gap + duplicate suppression + reconnect + terminal-state done frame + tenant-auth + bounded polling); GET /automation/metrics (derived from durable records, never mutable counters); GET /operations/retry-status; GET /operations/health/p2b. HTTP semantics: 401/403/404 (no cross-tenant existence leak)/409 (fenced version conflict surfaces as 409 not 500)/422/429/5xx-only. No stack traces/secrets/credentials exposed. Tenant from authenticated principal never body. Destinations masked in output; redactSecrets strips bearer/userinfo/token= from errors.
- Dependency direction (enforced, no cycles): artifacts → {store-sql, audit}; delivery → {audit}; ops → {store-sql, audit}; scheduler → {automation (types), store-sql}; recovery → {automation, ops, store-sql, audit}; control → {automation, scheduler, ops}. Runner NOT modified, never imports business/product. Hard seam holds.
- AutomationStore extended (backward compatible): listExpiredApprovals, listAbandonedRuns, listFailedDeliveriesForRetry (tenant-wide operational listing for the recovery scanner; reuse store interface, no raw SQL — resolves PGlite `$1` vs sqlite `?` placeholder issue). SqlScheduleStore gains listSchedules(tenantId, orgId?, projectId?).
- New migration: scheduler `schedule_core` (schedules + schedule_versions + schedule_occurrences; UNIQUE occurrence_id; fenced current_version CAS on every state-changing write). ops `ops_core` (ops_work_items; UNIQUE (tenant, idempotency_key); fenced generation on claim/complete).
- Invariants that must not regress: artifact references never trust arbitrary tenant-supplied paths (content-addressed); no SSRF (internal-network blocked before outbound); no plaintext secrets in logs/audit/events/errors (redactSecrets/redactUrl); no cross-tenant read (404, no existence leak) on every Phase 2B route including SSE; a crashed worker is safely replaceable (fenced generation — superseding generation can never complete superseded work, across connections/partition); a scheduler crash never duplicates a run (deterministic occurrenceId + UNIQUE); no retry silently duplicates a non-idempotent external action (in_progress before provider call; UNIQUE idempotency boundary); recovery never invokes agent execution (reads durable state, re-projects, re-drives via idempotent reconcileRun); exactly-once only at durable identity boundaries (schedule occurrence UNIQUE, ops work UNIQUE (tenant,idempotencyKey), delivery UNIQUE run_id+idempotency_key); execution stays at-least-once; no new authorization model (reuses Phase 1E role-rank); no runner coupling to business/product introduced.
- PG validation: Tier A PGlite (real PostgreSQL engine, ALWAYS runs) proves SQL-level invariants for artifacts/ops/scheduler/recovery + control-plane routes (8 Phase 2B control-plane tests pass over PGlite). Tier B multi-connection server tests remain environment-gated on PG_TEST_* (skipped honestly). PG skips reported as skips, never passes.

Phase 2C COMPLETE: 345 passed / 18 environment-gated skips (10 PG + 7 Docker + 1 pglite-server) / 0 failures. Zero TypeScript errors (tsc --build). report docs/phase2c.md; OpenHands study docs/phase2c-openhands-study.md.
- New packages: vaulltcore-credentials (durable ConnectionStore + CredentialResolver + ProviderCredential + ConnectionCapability; replaceable SecretProvider seam — never plaintext, only SHA-256 secretFingerprint; lifecycle create/inspect/rotate/revoke/expire/refresh/disconnect/last-used; rotate changes secret not connection identity; resolve is the ONLY boundary where a usable secret crosses into an adapter; revoked/disconnected/expired resolve to null; last-used best-effort never an auth source), vaulltcore-integration (neutral IntegrationProvider seam + ProviderRegistry; shared verifyHmacSha256 constant-time handling sha256=/v0= forms; deterministicEventId sha256(tenant|provider|providerEventId); SSRF-guarded ProviderHttpClient; classifyResponse 6 retry classes; NormalizedEvent + RawWebhook + WebhookVerifyResult + ExternalMutation idempotency identity), vaulltcore-git (neutral GitProvider + GitHub + GitLab adapters: OAuth/App identity, repo list/metadata, branches, file read/write, commits, branch+PR create/inspect, issue read/create/update, HMAC webhook verify + event normalize; idempotent PR creation via GET-search-before-POST; no GitHub types leak into the generic contract), vaulltcore-connectors (neutral PmProvider seam + Linear GraphQL adapter + Slack connector; webhook verify + normalize; reuses Phase 2B delivery guarantees — never bypasses them; generic PM seam so Jira etc. add without redesign), vaulltcore-models (BYOK: ModelRegistry to CredentialResolver to ModelProviderAdapter; OpenAI-compatible + Anthropic + Google adapters over the SSRF-guarded HTTP seam — NO provider SDK is a core dependency; immutable ModelDescriptor cost metadata — pricing changes ship a new descriptor never rewrite history; tenant ModelRestrictions enforced; usage events compatible with Phase 1E metering; error normalization 401->auth_config/429->rate_limited/5xx->transient; the existing AgentEngine/ModelProvider seam stays authoritative — NO second agent runtime, models package never depends on the runner), vaulltcore-webhooks (durable WebhookGateway: verify->resolve->tenant-auth->dedupe->persist->enqueue->audit, NEVER executes an agent in the request path; SqlWebhookStore over SqlStoreBase/PGlite with UNIQUE (tenant_id,event_id) dedup linearization point — duplicate webhook returns duplicate and never re-enqueues; timestamp stale/future replay rejection; raw-event quarantine for forensics; SubscriptionMatcher provider-neutral fan-out with deterministic triggerKey trig:tenant:subscription:eventId).
- Dependency direction (enforced, no cycles): credentials -> {identity, store-sql, audit}; integration -> {credentials (types), audit}; git -> {integration, credentials, delivery}; connectors -> {integration, credentials, delivery}; models -> {credentials, integration}; webhooks -> {integration, credentials, audit, store-sql}. Runner imports NONE of these. AgentEngine/ModelProvider seam preserved.
- Only Phase 1 file touched: vaulltcore-audit/src/contracts.ts — purely additive (10 new integration event types appended to AUDIT_EVENT_TYPES const: integration_connected/disconnected/revoked/rotated/credential_refreshed, webhook_accepted/rejected, external_mutation, provider_failure, byok_usage; type persisted as TEXT, no schema change). No Phase 1/2A/2B semantic invariant weakened.
- Invariants that must not regress: secrets NEVER appear in API responses/logs/audit metadata/errors/list-get endpoints (only SHA-256 fingerprint stored); tenant identity derived from authenticated principal (control) or route/signature secret (webhooks) NEVER request body; a webhook request NEVER executes an agent (persist + enqueue, process async); a duplicate webhook NEVER creates duplicate automation work (UNIQUE eventId linearization; execution at-least-once, exactly-once only at durable identity boundaries — event/ledger/run); no cross-tenant existence leak (resolver/store returns null on mismatch; 404 indistinguishable from absence); webhook forgery -> unverified + nothing persisted; webhook replay -> dedup + timestamp rejection; SSRF blocked before outbound (loopback/RFC1918/link-local; URL userinfo stripped); no provider SDK in core (narrow HTTP seam only); no second agent runtime (AgentEngine authoritative); no second authorization model (reuses Phase 1E role-rank); provider execution NEVER claimed exactly-once (at-least-once; exactly-once only at durable identity boundaries: webhook dedup tenant+provider+providerEventId, external mutation tenant+connectionId+operationId, automation trigger tenant+subscriptionId+eventId, automation run tenant+runId+idempotencyKey); recovery NEVER invokes agent execution to repair integration projections; OpenHands NOT a runtime dependency, source NOT reproduced.
- OpenHands study (docs/phase2c-openhands-study.md): adopted capabilities (token validation -> CredentialResolver+family; GitHub/GitLab App install -> git adapters; litellm provider/model BYOK -> ModelRegistry+ModelDescriptor with apiBase from connection metadata; webhook event triggering -> durable gateway + matcher; custom webhook signature headers -> provider adapters + verifyHmacSha256). Rejected: OpenHands runtime dep; reproducing source; provider SDKs in core; flattened single-LLM abstraction; agent execution from webhook request; exactly-once provider execution; ambient secrets as env vars; second agent runtime; hard-coded secret vendor.
- Validation tiers (honest): proven locally (345 passed over fake HTTP/SSE + PGlite); provider-conformance tested (neutral suites exist, live vendor gated); provider-dependent (live GitHub/GitLab/Linear/Slack/OpenAI/Anthropic/Google — deferred to Phase 2D harness); deferred (control-plane phase2c routes, OAuth callback handler, model discovery, trigger->run dispatch wiring).
- Phase 2D recommendations: control-plane phase2c HTTP routes (connections/credentials-metadata/OAuth/repos/providers/webhook-status/subscriptions/BYOK-models/integration-health); OAuth callback handler (state issuance + code exchange + state replay protection); live provider conformance harness (env-gated on GITHUB_TEST_TOKEN/etc, honestly skipped); bridge ModelStreamEvent -> runner ModelProvider adapter (above the runner, runner unchanged); webhook trigger -> AutomationService run dispatch with idempotency_key derived from triggerKey.

Phase 2D COMPLETE: 375 passed / 25 environment-gated skips (18 baseline: 10 PG + 7 Docker + 1 pglite-server; + 7 live-conformance: GitHub/GitLab/Linear/Slack/OpenAI/Anthropic/Google) / 0 failures. Zero TypeScript errors (tsc --build). report docs/phase2d.md.
- New code (additive, behind existing seams): vaulltcore-credentials/src/lifecycle.ts (durable OAuth ConnectionLifecycle + oauth-adapter.ts OAuthAdapterRegistry/OAuthProviderAdapter + oauth-store.ts SqlAuthorizationAttemptStore), vaulltcore-automation/src/trigger.ts + trigger-store.ts (immutable versioned TriggerDefinition + revisions + SqlTriggerStore) + dispatch.ts (TriggerDispatchService + TriggerRunSink seam), vaulltcore-models/src/connection-service.ts (ModelConnectionService BYOK activation), vaulltcore-control/src/phase2d-routes.ts (PHASE2D_ROUTES: /connections, /oauth/callback, /triggers, /integrations/capabilities, dispatches), per-adapter test/live-conformance.test.ts (Tier C env-gated).
- Connection lifecycle state machine (explicit transitions, deterministic 409 on invalid): disconnected -> authorization_pending -> authorization_verified -> active -> degraded -> expired -> revoked -> disconnected. OAuth state bound to tenant/org/project/principal/provider/attempt BEFORE redirect; callback is unauthenticated at the edge, tenant resolved FROM the durable state record (getByStateGlobal), NEVER request body/query. One-time state settlement (consume on first use); replay-safe (duplicate callback returns original outcome, no contradictory state). PKCE where supported. Credential persistence only via SecretProvider (secretRef + sha256 fingerprint, never plaintext). verifyIdentity after exchange; activate ONLY after verification. refresh rotates secret (identity stable); refresh failure -> degraded (recoverable, not revoked). revoke()=terminal revoked; disconnect()=disconnected (re-authorizable). CredentialResolver.resolve() returns null for revoked/disconnected/expired.
- Trigger model: immutable versioned TriggerDefinition (triggerId + revision; any change = new revision; checksum verified on load); triggerRevision PINNED into every dispatch so historical match stays explainable (a newer revision never reinterprets a past match). Declarative deterministic matching ONLY (provider + eventKinds + resourcePattern glob + optional action + optional connectionId + selectors) — no arbitrary JS/user code. Trigger classes: webhook_event/schedule/manual/integration_event. Schedule reuses Phase 2B scheduler (scheduleId) — NO second scheduling system. enabled/disabled fenced by revision CAS.
- Trigger -> run dispatch: TriggerDispatchService operates AFTER durable webhook persistence. reserveDispatch is the linearization point: UNIQUE (tenant_id, source_event_id, trigger_id) — the exactly-once boundary (one dispatch per event/trigger identity). driveDispatch honors disabled triggers (rejected, no run), policy/quota rejection (honest terminal, kind policy/quota/invalid_input/disabled_trigger, NEVER retried as infra), retryable infra failure (retryable_failure, re-driveable). redrive re-drives non-terminal dispatches idempotently (unique dispatchId -> no duplicate); dead-letters honestly after maxAttempts. Drives run creation through narrow TriggerRunSink seam (control plane implements over AutomationService.createRun) — dispatch.ts NEVER imports the runner.
- Model/BYOK activation: ModelConnectionService register/verifyConnectivity/activate/deactivate/revoke; verifyConnectivity is a bounded explicit one-token probe (NOT automatic model discovery); secret crosses resolver boundary only transiently, never logged/returned/audited. tenant ModelRestrictions (allowedProviders/allowedModels/maxInput/maxOutput) enforced by ModelRegistry at resolve. Model selection: ModelRegistry -> CredentialResolver -> ModelProviderAdapter -> existing AgentEngine/ModelProvider seam. NO second LLM abstraction; models package NEVER depends on the runner.
- Migration ledger: migration NAME globally unique (Phase 1F dedup-by-name preserved). New tables: oauth_authorization_attempts (state nonce UNIQUE, PKCE, binding, expiry, one-time outcome), automation_triggers (revision + checksum, UNIQUE (tenant,org,project,name)), automation_trigger_dispatches (UNIQUE (tenant_id, source_event_id, trigger_id), fenced state transitions, attempts/last_error). Dead-letter = terminal dead_letter dispatch state (no separate table). No secrets in these tables.
- Only Phase 1 file touched: vaulltcore-audit/src/contracts.ts — purely additive (Phase 2D audit event types appended to AUDIT_EVENT_TYPES const: connection_authorization_started/verified/activated/degraded/refreshed/revoked/disconnected, callback_rejected, trigger_published/rejected/dispatched/dead_lettered, model_connection_activated/deactivated; type persisted as TEXT, no schema change). No Phase 1/2A/2B/2C semantic invariant weakened.
- Invariants that must not regress: OAuth state one-time + consumed (no replay); expired/wrong-binding authorization rejected; duplicate callback idempotent (no contradictory state); cross-tenant/cross-project-no-grant connection access = 404 (no existence leak); secrets NEVER in responses/audit/errors (only secretRef + sha256 fingerprint); refresh failure -> degraded/expired (recoverable not revoked); revoked/disconnected/expired resolves null; forged webhook -> unverified + nothing persisted; duplicate webhook -> no duplicate dispatch (UNIQUE dispatch identity); one event N triggers -> N dispatches (one per trigger identity); crash after reservation -> re-drive idempotent (no duplicate dispatch); policy rejection NEVER starts execution; quota rejection NEVER starts execution; retryable infra re-drives safely; disabled trigger creates no run; historical trigger match explainable after update (revision pinned); model connection tenant/project safe; live tests skip honestly when unconfigured; no SSRF (loopback/RFC1918/link-local blocked, userinfo stripped); no provider SDK in core; no second agent runtime; no second authorization model; execution stays at-least-once (exactly-once ONLY at durable identity boundaries: webhook tenant+provider+providerEventId, dispatch tenant+sourceEventId+triggerId, run tenant+runId+idempotencyKey, delivery tenant+runId+idempotency_key, schedule occurrence occ:scheduleId:scheduledTime); recovery NEVER invokes agent execution, NEVER bypasses admission, NEVER duplicates a committed dispatch identity, NEVER reinterprets historical trigger definition using newer revision; OpenHands NOT a runtime dependency, source NOT reproduced.
- Dispatch identity fix (Phase 2D): the trigger dispatch UNIQUE constraint is (tenant_id, source_event_id, trigger_id), NOT trigger_revision — two distinct triggers both at revision 1 must each get their own dispatch. The duplicate-lookup query uses trigger_id. Do not revert to trigger_revision (it collapses distinct triggers).
- Validation tiers (honest): Tier A deterministic (fakes/PGlite, ALWAYS run); Tier B provider-neutral contract (every adapter satisfies); Tier C live vendor (env-gated on GITHUB_TEST_TOKEN/GITLAB_TEST_TOKEN/LINEAR_API_KEY/SLACK_TEST_TOKEN/OPENAI_TEST_API_KEY/ANTHROPIC_TEST_API_KEY/GOOGLE_TEST_API_KEY + dedicated test resource, honestly skipped). A skipped live test is a SKIP, never a fake pass. No failing test converted to a skip. Live providers validated: NONE actually ran (all env-gated skips in the sandbox); conformance suites exist and are wired.
- Phase 2E recommendations: full control-plane HTTP integration tests over PGlite for Phase 2D routes; OAuth callback signed short-TTL cookie bound to principal session (in addition to durable state record); safe declarative filter expression subset (currently literal + glob, no JMESPath) after security review; webhook trigger -> AutomationService run dispatch wiring with idempotency_key derived from dispatch identity; live provider conformance expanded to write-then-cleanup flows behind the same env gates.
Phase 2E COMPLETE: 394 passed / 25 environment-gated skips (10 PG + 7 Docker + 1 pglite-server + 7 live-conformance) / 0 failures. Zero TypeScript errors (tsc --build). report docs/phase2e.md.
- New package: `packages/vaulltcore-reliability` (ReliabilityReconciliationService bounded+watermarked+concurrent-safe+idempotent, never invokes agent execution; RedriveService authorized+tenant-isolated+idempotent never resurrects terminal; TimeoutService durable cooperative cancel/timeout fenced by runVersion CAS; TelemetrySink/AuditTelemetrySink redacted; HealthService/SqlStorageProbe readiness+tenant health). Depends on ops/automation/quota/audit/store-sql. Runner NOT modified, never imports reliability. Hard seam holds.
- Extensions (additive, backward compatible): ops store migration `ops_reliability` (v2) adds `dead_letter` terminal state + redrive/listDeadLettered/listPendingBatch/countByState/reapExpiredClaims; complete() exhausted retries -> dead_letter (distinct from failed_terminal); audit contracts extended with Phase 2E telemetry event types (TEXT persisted, no schema change). automation trigger store migration `automation_dispatch_lease` (v4) adds redrive_generation/owner/expires_at + claimRedriveDispatch/renewRedriveLease/releaseRedriveLease/redriveDeadLetter/listStrandedDispatches/listDeadLetteredDispatches; dispatch service redrive() now operates under a fenced lease, terminal dispatches never claimed. quota store migration `quota_global_capacity` (v5) adds a global concurrent capacity ceiling (QUOTA_GLOBAL_FULL honest reject, per-scope claim rolled back); global decrement in settle/release/reapExpired; setGlobalCapacity/getGlobalUsage.
- Control plane: `packages/vaulltcore-control/src/phase2e-routes.ts` (PHASE2E_ROUTES: retry-status, dead-letter redrive ops+dispatch, reconcile, timeout-scan, runs/:id/cancel, readiness, operations/health/reliability) wired into server dispatch additively (phase2e layer option; matched before generic routes).
- Invariants that must not regress: a process being alive is never proof it still owns work (every recoverable worker-owned op has a durable fenced lease: ops claim generation, dispatch redrive generation, quota reservation version); a stale worker (older generation/version) can NEVER finalize/overwrite/transition work after a newer generation took over (CAS fencing); policy/quota/auth/validation/cancelled/timeout rejections are NEVER retried as infrastructure (explicit FailureClass); retries bounded+persisted (nextRetryAt, attempt history, sanitized lastError 500 cap), recovery after restart derives pending retries from durable state alone (no in-memory timers as source of truth); exhausted/poisoned work -> explicit terminal dead_letter, operator redrive is authorized+tenant-isolated+idempotent+never resurrects terminal+never creates duplicate durable identities+audited, permanent policy/auth/quota rejection never auto-retried; reconciliation safe to run repeatedly+concurrently, every repair fenced/idempotent, never scans-and-blindly-rewrites, bounded batch+continuation (never unbounded DB op), never invokes agent execution (reconcileRun re-projects+re-drives idempotently, dispatcher deduplicates on (runId,stepId)); per-tenant + global capacity ceilings, capacity released on terminal completion (both counters), leaked capacity recovered after crashes, one tenant cannot consume another tenant's reserved capacity, delayed work gets honest state never silently dropped; cancellation/timeout race behavior durable-ordered (fenced CAS) not process-timed, late worker never resurrects cancelled/terminal work; telemetry NEVER emits credentials/auth headers/API keys/tokens/secret references/unrestricted raw payloads (sanitizeMetadata + metadata builders never put secrets in); cross-tenant reads return 404 (no existence leak); readiness does NOT depend on external providers (provider outage degrades only affected work); exactly-once ONLY at durable identity boundaries (execution stays at-least-once).
- Migration ledger: migration NAME globally unique (Phase 1F dedup-by-name preserved). New migrations: ops v2 `ops_reliability`, automation v4 `automation_dispatch_lease`, quota v5 `quota_global_capacity`. No destructive migration. All state-changing writes remain fenced by CAS.
- node:sqlite ExperimentalWarning still suppressed in the `test` npm script via NODE_OPTIONS=--disable-warning=ExperimentalWarning (targeted, only ExperimentalWarning).
- Proven by Tier A deterministic tests (19 new in vaulltcore-reliability, in-memory SQLite + real durable layer): lease crash/expiry/stale-completion, retry exhaustion->dead_letter, redrive idempotency, reconciliation idempotency+safe racing, capacity release/recovery/isolation/global ceiling, telemetry redaction, restart recovery, bounded batch continuation. Requires future live validation: multi-connection PostgreSQL server races (Tier B env-gated), live provider conformance (Tier C env-gated), webhook trigger->run dispatch end-to-end under load.

Phase 2F COMPLETE: 437 passed / 25 environment-gated skips (10 PG + 7 Docker + 1 pglite-server + 7 live-conformance) / 0 failures. Zero TypeScript errors (tsc --build). report docs/phase2f.md.
- New package: `packages/vaulltcore-usage-governance` (UsageQueryService bounded+paginated+filtered aggregation over the immutable ledger; VersionedCostCatalog + attributeCost provider-neutral cost seam where usage quantity stays authoritative, unknown pricing is honestly null, pricing versions traceable, future price never rewrites history; QuotaSettlementService settles admission reservation against ACTUAL metered usage idempotently+fenced -- no second quota authority; jobAttributionProvider resolves public provider/model from job spec, never credentials). Depends on store-sql/metering/billing/quota. control -> usage-governance. Runner NOT modified, never imports business/product. Hard seam holds.
- Metering extended (backward compatible): `usage_events` gains `provider`/`model` columns (public identifiers only, never secrets); migration `metering_attribution`; AccountingIdentity builders (tokens/modelStep/tool/duration/snapshot/providerRequest) deterministic from committed event seq; validateUsageInput (reject negative/non-int/unknown-kind/bad-unit); eventsToUsageAttributed (identical dedup keys to eventsToUsage so attribution never double-accounts); bounded queryEvents/aggregateFiltered/breakdownByKind with MAX_QUERY_LIMIT + cursor pagination.
- Billing extended (backward compatible): migration `billing_adjustments` adds original_entry_id/reason/note; recordAdjustment is APPEND-ONLY (own accounting identity, UNIQUE (scope,idempotency_key), references original, never mutates); getEntry/listAdjustmentsFor; DEFAULT_PRICING; historical pricing immutable (a new version never rewrites a settled entry).
- Control plane: `packages/vaulltcore-control/src/phase2f-routes.ts` (PHASE2F_ROUTES: GET /usage, /usage/summary, /usage/runs/:id, /usage/ledger, POST /usage/reconcile [admin only]) wired additively (phase2f layer option; matched before generic routes). 401/404 no-leak/422 bounded filters+oversized limit+unbounded range/409/secrets-never-returned.
- Audit extended (additive only): `vaulltcore-audit/src/contracts.ts` gains Phase 2F event types (usage_reconciliation_requested/completed, usage_drift_detected, usage_adjustment_committed; TEXT persisted, no schema change). No Phase 1-2E semantic invariant weakened.
- Invariants that must not regress: execution stays at-least-once; accounting is exactly-once ONLY at durable accounting identity boundaries (usage_events UNIQUE (tenant_id,job_id,kind,dedup_key); ledger_entries UNIQUE (scope,idempotency_key)); a retry re-deriving usage over committed history produces the SAME identities -> ON CONFLICT DO NOTHING collapses to one charge (no double-accounting); aggregates/summaries/cost-attributions are DERIVED, never authoritative (recomputed from the immutable ledger); cost is derived metadata, never a substitute for usage quantity, unknown pricing is honestly null; pricing identity (pricingId+version+effectiveAt) traceable, future price never rewrites history; corrections are APPEND-ONLY adjustments referencing original_entry_id, never silent UPDATE; quota settlement against actuals is idempotent+fenced (no double-settlement, no negative balance, no releasing settled, no cross-tenant, no fabricated usage for no-linked-job reservation); policy/quota rejection stays honest+terminal (never retried as infra); attribution carries NO credentials (public provider/model only); reconciliation NEVER invokes agent execution (re-projects + re-drives idempotently); cross-tenant reads/settlement return 404/empty (no existence leak); tenant from authenticated principal never body; no float for authoritative quantities (int tokens + int micro-currency); no secrets in ledger/audit/logs/errors/responses.
- Validation: Tier A deterministic over PGlite (real PostgreSQL engine, ALWAYS runs) -- 43 new tests (14 metering + 14 usage-governance + 5 billing + 10 control-plane). Tier B multi-connection PG server env-gated on PG_TEST_* (skipped honestly). Tier C live provider conformance env-gated (honestly skipped). PG/live skips reported as skips, never passes.

Phase 2G COMPLETE: 467 passed / 25 environment-gated skips (10 PG + 7 Docker + 1 pglite-server + 7 live-conformance) / 0 failures. Zero TypeScript errors (tsc --build). report docs/phase2g.md.
- New package: `packages/vaulltcore-auth` (Authority split: Better Auth owns session authentication; Vaulltcore owns tenant/org/membership/permission authorization. `BetterAuthAdapter` is the ONE narrow seam; domain packages never import better-auth. `ActorResolver` resolves session-cookie → session fingerprint registry lookup → membership → `ResolvedPrincipal`; bearer → machine credential or legacy API key. Central `Permission` catalog + `authorize()`; changes: role, membership, disable, revocation take effect AT NEXT REQUEST (per-request lookup). `ServiceIdentityService` explicit bounded permission subset; creators must hold granted permissions. Durable `SqlB2bAuthStore` with session revocation ledger keyed by SHA-256 fingerprint — never raw tokens). Depends on identity/store-sql/audit + better-auth. control -> auth.
- New migration: `b2b_auth_core` (user_identity + service_identities UNIQUE (tenant,org,name) + machine_credentials metadata + session_registry fingerprints). Better Auth tables run via its own Kysely migrations through `BetterAuthAdapter.migrate()`.
- Control plane additive `phase2g` layer: public `/auth/*` bridge and protected `/identity/*` pipeline (me/permissions/members/service-identities/sessions/users disable/revoke-all). Error semantics: 401/403/404/409/422; no secrets; deterministic.
- Better Auth CSRF trap: NODE_ENV=test defaults skip-origin-check — adapter explicitly overrides `advanced: { disableOriginCheck: false, disableCSRFCheck: false }`. Never weaken this.
- Only Phase-1-style cross-cutting touch: `vaulltcore-audit/src/contracts.ts` appended Phase 2G event types (TEXT, no schema change); `vaulltcore-identity/src/store.ts` gained read-only `getMember`. No semantic weakening.
- Invariants that must not regress: session ≠ permission; every protected request resolves a validated Actor before authorization; actor carries attribution never secrets; client-supplied org hint validated server-side; revocation checked at validation time; machine credentials fingerprint-only, secret shown once; creators cannot grant permissions they lack; single request security pipeline with explicit public exceptions (/health+/auth/*); audit metadata fingerprints-only; per-request membership/role lookup (no embedded-claims authz); test had to be rewritten when NODE_ENV shocked BA's origin check — keep the override.


